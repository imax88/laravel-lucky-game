# Laravel Lucky Game

A Laravel-based game project running in Docker (Apache + PHP 8.2 + SQLite).

##  Quickstart

```bash
git clone https://github.com/imax88/laravel-lucky-game.git
cd laravel-lucky-game
docker-compose up --build
